#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integer
 * @a: integer one
 * @b: integer two
 * Return: always 0
*/

int mul(int a, int b)
{
	int x = a * b;

	printf("%d", x);
	return (0);

}
