#include <stdio.h>

/**
 * main - entry point
 * @argc: count in array
 * @argv: array of string
 * Return: always 0
*/

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
